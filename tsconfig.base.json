{
    "compilerOptions": {
        /* Language and Environment */
        "target": "esnext",

        /* Modules */
        "module": "nodenext",
        "moduleResolution": "nodenext",

        /* Emit */
        "declaration": true,
        "declarationMap": true,
        "removeComments": true,
        "noEmitOnError": true,

        /* Interop Constraints */
        "verbatimModuleSyntax": true,

        /* Type Checking */
        "strict": true,

        /* Completeness */
        "skipLibCheck": true,

        /* Projects */
        "composite": true
    }
}
