# Hyperion

Base Template for SSG + CloudFlare Workers

## Agent Guidelines

Shared agent instructions live in `AGENTS.md`, `CLAUDE.md`, and `GEMINI.md`. Keep all three copies in sync whenever project context, stack, workflow, permissions, command boundaries, or skills change.

## Template Merges

Use `MERGING.md` when bringing updates from this global template into existing forks.
