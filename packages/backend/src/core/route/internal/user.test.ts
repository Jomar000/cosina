import { describe, expect, it } from 'vitest'

describe('/internal/user Endpoint Unit Tests', () => {
    it('/signIn: Signing-in with valid credentials.', () => {
        expect(1).toEqual(1)
    })
})
