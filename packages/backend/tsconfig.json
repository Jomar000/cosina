{
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
        /* Modules */
        "rootDir": "./src/",
        "types": [
            "@cloudflare/vitest-pool-workers",
            "node"
        ],

        /* Emit */
        "outDir": "./dist/"
    },
    "include": ["src/**/*"]
}
