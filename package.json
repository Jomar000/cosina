{
    "name": "hyperion",
    "version": "0.0.1",
    "author": "4thDEVisionTech",
    "description": "Hyperion",
    "type": "module",
    "private": true,
    "license": "ISC",
    "engines": {
        "node": ">=24.16.0",
        "pnpm": ">=11.8.0"
    },
    "scripts": {
        "dev": "pnpm run --recursive --parallel --if-present dev",
        "build": "pnpm run --recursive --stream --if-present build",
        "format": "pnpm run --recursive --stream --if-present format",
        "lint": "pnpm run --recursive --stream --if-present lint",
        "prepare": "pnpm exec husky",
        "test": "echo 'Run tests per API app; concurrent and sequential Vitest commands run in parallel on isolated databases'"
    },
    "devDependencies": {
        "@changesets/cli": "^2.31.0",
        "@eslint/compat": "^2.1.0",
        "@eslint/js": "^10.0.1",
        "@types/node": "^24.13.2",
        "eslint": "^10.5.0",
        "eslint-config-prettier": "^10.1.8",
        "eslint-plugin-better-tailwindcss": "^4.6.0",
        "eslint-plugin-svelte": "^3.19.0",
        "globals": "^17.6.0",
        "husky": "^9.1.7",
        "prettier": "^3.8.4",
        "prettier-plugin-multiline-arrays": "^4.1.9",
        "prettier-plugin-svelte": "^4.1.1",
        "prettier-plugin-tailwindcss": "^0.8.0",
        "tsx": "^4.22.4",
        "typescript": "^6.0.3",
        "typescript-eslint": "^8.61.1",
        "wrangler": "^4.102.0"
    }
}
